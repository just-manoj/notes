class Notes {
  constructor(id, title, note, date, bgColor) {
    (this.id = id),
      (this.title = title),
      (this.note = note),
      (this.date = date),
      (this.bgColor = bgColor);
  }
}

export default Notes;
